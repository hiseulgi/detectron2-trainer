DATALOADER:
  NUM_WORKERS: 8
MODEL:
  DEVICE: cuda # cuda or cpu
  ROI_HEADS:
    NUM_CLASSES: 1 # person only
    BATCH_SIZE_PER_IMAGE: 128
SOLVER:
  BASE_LR: 0.01
  IMS_PER_BATCH: 16 # BATCH SIZE (2 - 16)
  WARMUP_ITERS: 379
  MAX_ITER: 1140
  STEPS: [379, 759]
TEST:
  EVAL_PERIOD: 57
